# schedule to amd64, avoid arm
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/instance: argo-cd
spec:
  template:
    spec:
      nodeSelector:
        kubernetes.io/arch: amd64
