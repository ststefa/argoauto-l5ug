apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: argo-route
  namespace: argocd
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`argo.l5ug.nom.cx`)
      priority: 10
      services:
        - name: argocd-server
          port: 80
