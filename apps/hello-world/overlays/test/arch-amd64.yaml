apiVersion: apps/v1
kind: Deployment
metadata:
  name: simple-deployment
  #! Not really required, target already unique
  labels:
    app.kubernetes.io/instance: argo-cd
spec:
  template:
    spec:
      nodeSelector:
        kubernetes.io/arch: amd64
